<?xml version="1.0" encoding="utf-8"?>
<!--
  SonicSound connects to self-hosted Subsonic/Navidrome servers.

  Android Network Security Config cannot express private LAN CIDRs (e.g. 192.168/16),
  so cleartext remains permitted app-wide for LAN HTTP. Prefer an https:// account URL
  when your server supports TLS — no app setting required.

  Localhost / .local are listed explicitly for clarity; they do not cover LAN IPs.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">local</domain>
    </domain-config>
</network-security-config>
