# Deferred desktop shell (Tauri)

This tree is kept for a future desktop milestone. It is **not** wired into
`package.json` scripts or CI. Active targets are Android (phone + TV) and the
Vite web/PWA build.
